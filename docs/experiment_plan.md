# Experiment Plan

H1: Run `demo_two_agents.py` and inspect typed frame flow.
H2: Run capability tests and `bench_capability_enforcement.py`.
H3: Run audit DAG tests for missing parents and tampering.
H4: Run `bench_signature_overhead.py` and `bench_latency.py`.
H5: Run `bench_payload_size.py`.
H6: Run `test_transport.py` for LocalTransport abstraction.

Raw results are stored under `benchmarks/results/`. Figures are generated by `scripts/generate_figures.py`.
