\documentclass[conference]{IEEEtran}
\usepackage{cite}
\usepackage{graphicx}
\usepackage{booktabs}
\usepackage{url}
\usepackage{array}
\usepackage{listings}

\lstset{basicstyle=\ttfamily\scriptsize, columns=fullflexible, breaklines=true, frame=single}

\title{SIFR: Secure Interchange for Federated Reasoning --- A Prototype Protocol for Structured and Verifiable AI-Agent Communication}

\author{\IEEEauthorblockN{Author One, Author Two}
\IEEEauthorblockA{Department / University Placeholder\\
Email: author@example.edu}}

\begin{document}
\maketitle

\begin{abstract}
Current AI-agent systems rely heavily on web-era protocols and ad hoc message formats, including HTTP APIs, JSON-RPC, Server-Sent Events, WebSockets, plain JSON function calls, and free-form natural language messages. These mechanisms are useful and widely deployed, but they were not designed as a native substrate for autonomous agents that must prove identity, exchange constrained capabilities, invoke tools safely, and preserve auditable reasoning and execution lineage. This paper presents SIFR, Secure Interchange for Federated Reasoning, in two iterations. SIFR v0.1 was a feasibility prototype combining signed typed frames, explicit capability grants, and a content-addressed audit DAG, with a complete two-agent vertical slice over a local in-process transport. v0.1 honestly listed QUIC transport, WASM isolation, DID resolution, Verifiable Credentials, revocation, replay protection, and formal analysis as future work.

SIFR v0.2 promotes the prototype to a verified research artifact that implements and evaluates each of those layers. We add: encrypted-at-rest key management with rotation and revocation metadata (Argon2id KDF, AES-256-GCM with kid-bound AAD); DID resolution for did:web (over HTTP/HTTPS) and a local-only did:sifr method; VC-inspired capability credentials with full mutation defenses (no W3C VC compliance claimed); signed capability revocation with on-load signature re-verification; replay protection bound to (sender, session, message\_id) over a sliding timestamp window; sandboxed WASM tool execution via wasmtime with no WASI imports linked and per-call fuel limits; a real QUIC transport via aioquic with ALPN-negotiated connections; an 11-attack controlled adversary evaluation; and a TLA+ model of the capability authorization state machine, model-checked with TLC across 276{,}205 distinct states under 7 invariants. The end-to-end vertical slice runs over real QUIC and exits with the spec's expected output (\texttt{Result: 5}, \texttt{Demo completed successfully.}). The artifact reports v0.1 microbenchmarks (signed SIFR action 411 bytes vs 53 bytes plain JSON; Ed25519 sign 0.0314 ms; verify 0.0615 ms; sign+verify+DAG 0.1104 ms; all on Python 3.14.2, Windows 11) alongside v0.2 measurements (DID:sifr resolve 0.07 ms warm; QUIC RTT 0.94 ms; WASM warm 113 us; reject latency for the 11 attack classes 2 us-2.4 ms). We make explicit, auditable claims about what is implemented and tested, and what is NOT claimed: no W3C VC compliance, no cryptographic proof, no arbitrary-untrusted-code WASM safety, no production readiness.
\end{abstract}

\begin{IEEEkeywords}
AI agents, multi-agent systems, protocol design, capability security, Ed25519, audit logs, provenance, tool use
\end{IEEEkeywords}

\section{Introduction}
Large language model (LLM) agents are increasingly deployed as systems that reason, call tools, retrieve context, delegate subtasks, and coordinate with other agents. In practice, these interactions are often represented as text or JSON carried over web infrastructure. A typical system may encode a tool call as a JSON object, send it over HTTP, stream partial results through Server-Sent Events, or pass a natural language transcript from one agent to another. This approach is pragmatic, but it leaves important semantics outside the protocol layer: whether a message is a thought, action, observation, tool invocation, critique, error, or final result; whether the sender is authorized to request the action; whether the permission is bounded and revocable; and whether an auditor can later verify the lineage of the interaction.

Recent interoperability work has begun to address adjacent problems. The Model Context Protocol (MCP) standardizes how model hosts connect to tools, prompts, and resources over JSON-RPC \cite{mcp2024spec,anthropic2024mcp}. Agent2Agent (A2A) targets heterogeneous agent discovery and task exchange \cite{a2a2025spec,google2025a2a}. IBM/BeeAI's Agent Communication Protocol (ACP) explored HTTP-native agent messaging and has become closely related to the A2A ecosystem \cite{beeai2025acp,ibm2025acp}. The Agent Network Protocol (ANP) explores identity, encrypted communication, and protocol negotiation for open agent networks \cite{chang2025anp,anp2025didwba}. These efforts are valuable, but a research gap remains: a minimal, reproducible, agent-native frame design that binds typed semantics, cryptographic integrity, fine-grained capabilities, and content-addressed audit lineage in one executable prototype.

This paper investigates that gap through SIFR, Secure Interchange for Federated Reasoning. SIFR is not presented as a replacement for HTTP, MCP, A2A, ACP, or ANP, and v0.1 is not presented as a production security protocol. Instead, v0.1 asks a narrower research question: what does a verifiable agent communication vertical slice look like if every meaningful action must be represented as a signed typed frame, authorized by a signed capability grant, and recorded in an audit DAG?

The contributions of this work are:
\begin{itemize}
\item A protocol model for signed, typed AI-agent frames with explicit action, observation, result, error, critique, capability, and TensorFrame message types.
\item A minimal capability layer that binds issuer, subject, allowed actions, resource scope, expiration, budget, and delegation constraints.
\item A content-addressed audit DAG for verifiable message lineage.
\item A Python v0.1 prototype implementing the complete two-agent calculator workflow.
\item Unit tests, negative authorization and tampering tests, benchmark scripts, raw benchmark outputs, generated figures, and an Overleaf-ready IEEE-style artifact.
\end{itemize}

\section{Background and Related Work}
\subsection{Agent Communication Languages}
Structured agent communication predates LLMs. KQML represented agent messages using performatives for knowledge and query exchange \cite{finin1997kqml}. FIPA ACL specified message structures and communicative acts such as request and inform \cite{fipa2002aclmessage,fipa2002communicativeacts}. These systems established that agent messages benefit from explicit semantics. However, they were designed for symbolic multi-agent systems rather than modern LLM agents that call external tools, consume untrusted retrieved content, and execute generated workflows.

\subsection{Tool-Using LLM Agents}
ReAct showed that interleaving reasoning traces and actions can improve language model performance on tasks requiring environment interaction \cite{yao2023react}. Toolformer demonstrated that language models can learn when and how to call external tools \cite{schick2023toolformer}. TalkHier argues that structured communication and hierarchical refinement can improve multi-agent systems \cite{wang2025talkhier}. These works support the premise that agent communication should distinguish reasoning, actions, and observations. They do not, by themselves, define a cross-agent security or audit protocol.

\subsection{Modern Agent Interoperability Protocols}
MCP addresses the integration of models with external context and tools \cite{mcp2024spec,anthropic2024mcp}. A2A focuses on agent discovery, task exchange, modality negotiation, and enterprise interoperability \cite{a2a2025spec,google2025a2a}. ACP explored rich messages, streaming, long-running tasks, and shared state in an HTTP-native agent communication layer \cite{beeai2025acp,ibm2025acp}. ANP proposes identity-oriented agent networking with protocol negotiation \cite{chang2025anp}. SIFR differs in scope: it is a research prototype for signed frame semantics, capability enforcement, and audit DAG lineage. In future work, SIFR frames could be transported through or adapted to existing ecosystems, but v0.1 does not claim deployed interoperability.

\subsection{Security, Identity, and Provenance}
NIST Zero Trust guidance emphasizes per-request access decisions rather than implicit trust in the network perimeter \cite{rose2020zerotrust}. DID Core and Verifiable Credentials provide standards for decentralized identifiers and cryptographically verifiable claims \cite{w3c2022did,w3c2025vc}. SIFR v0.1 is inspired by this direction but implements only DID-style identifier strings; it does not implement DID resolution or VC verification. Merkle DAGs provide content-addressed structures where node identifiers are derived from content and links \cite{ipfs2025merkledag}. Supply-chain provenance work such as SLSA motivates attestable workflows and reproducible evidence \cite{slsa2025spec}.

\subsection{Transport and Sandboxing}
QUIC provides multiplexed streams and integrated transport security \cite{iyengar2021quic}, and HTTP/3 carries HTTP semantics over QUIC \cite{bishop2022http3}. SIFR v0.1 implements a local transport abstraction only; QUIC is a future backend. WebAssembly and WASI motivate portable sandboxing for tool execution \cite{w3c2019wasm,wasi2024interfaces}. v0.1 includes a safe Python calculator stub, not a WASM sandbox.

\subsection{Agent-Specific Risks}
Prompt injection research shows that LLM-integrated applications can be manipulated through untrusted content, including content that indirectly affects tool behavior \cite{greshake2023indirectpi,liu2024promptinjection}. OWASP LLM and MCP security guidance identifies tool poisoning, command injection, prompt injection, and unsafe tool execution as major risks \cite{owasp2025llmtop10,owasp2026mcptop10}. Embedding inversion and KV-cache leakage work cautions against assuming that latent or vector representations are private \cite{morris2023embeddingprivacy,luo2026shadowcache}. These risks motivate SIFR's explicit authorization and audit design, while also limiting what can be claimed from a v0.1 prototype.

\section{Problem Statement}
The central problem is not that HTTP or JSON are inadequate as byte carriers. The problem is that common agent communication stacks treat agent messages as application-defined objects whose semantics and authorization are often enforced inconsistently outside the communication layer. This creates at least five gaps.

First, message intent is ambiguous. A model-generated string may contain reasoning, a command, a partial observation, a critique, or a final answer, but the transport does not enforce a typed distinction. Second, identity and authorization are often ambient. API keys or bearer tokens may grant broad authority without binding the individual agent action to a constrained capability. Third, audit logs are frequently append-only text traces rather than cryptographically verifiable lineage structures. Fourth, tool execution may occur without a protocol-level proof that the action was allowed. Fifth, compact non-text channels such as embeddings or future latent representations require careful framing and privacy warnings.

SIFR v0.1 therefore tests whether a minimal protocol can bind the following invariant: every meaningful action is a signed typed message, checked against a signed capability, executed through a bounded tool interface, and recorded in a content-addressed audit DAG.

\section{Design Goals and Hypotheses}
The prototype evaluates six hypotheses:

\textbf{H1:} Structured signed agent messages provide clearer audit semantics than generic JSON messages.

\textbf{H2:} Capability grants can prevent unauthorized actions in a minimal two-agent workflow.

\textbf{H3:} An audit DAG can detect missing parents and message tampering.

\textbf{H4:} Signed SIFR frames introduce measurable overhead, but local Ed25519 cost is small enough for integrity-sensitive prototype workflows.

\textbf{H5:} TensorFrame base64 binary encoding can be more compact than naive JSON-list encoding for numeric vectors.

\textbf{H6:} A transport abstraction allows local testing while preserving an implementation boundary for a future QUIC backend.

\section{SIFR Protocol Architecture}
Figure~\ref{fig:architecture} summarizes the v0.1 layers. The implemented layers are the structured message layer, capability layer, cryptographic verification layer, audit DAG, TensorFrame demonstration encoding, calculator tool stub, and local transport. The future layers are QUIC transport, full sandboxing, DID/VC integration, and production interoperability bridges.

\begin{figure}[t]
\centering
\includegraphics[width=\linewidth]{figures/architecture.png}
\caption{SIFR v0.1 layered architecture. Implemented layers are local and prototype-scoped; QUIC, WASM, DID/VC, and full interoperability are future work.}
\label{fig:architecture}
\end{figure}

\subsection{Message Envelope}
Every SIFR message contains a protocol version, message identifier, session identifier, type, sender identifier, receiver identifier, UTC timestamp, parent CIDs, optional capability identifier, payload, and optional signature. The signature is computed over the canonical message without the signature field. The implemented message types are Hello, CapabilityOffer, CapabilityGrant, Thought, Action, ToolUse, Observation, Result, Critique, Error, and TensorFrame.

\subsection{Canonicalization and Signatures}
The prototype canonicalizes messages by removing the signature field, sorting JSON keys, using compact JSON separators, and encoding UTF-8. Ed25519 signatures are generated and verified using the \texttt{cryptography} library. Verification fails if the payload, sender, or any signed field changes after signing.

\subsection{Capabilities}
A CapabilityGrant payload binds an issuer, subject, allowed actions, resource scope, issue time, expiration time, call budget, payload-size budget, and delegation constraint. The action verifier checks the grant signature, issuer consistency, subject match, action membership, expiration, payload size, call budget, and delegation policy before tool execution.

\subsection{Audit DAG}
Each accepted message can be added to an audit DAG. The CID is computed as SHA-256 over canonical message bytes without the signature field. Nodes record CID, message id, message type, sender, receiver, parents, timestamp, and signature-valid status. The DAG verifier detects missing parents, invalid signature flags, and stored-message mutation.

\begin{figure}[t]
\centering
\includegraphics[width=\linewidth]{figures/handshake_sequence.png}
\caption{Two-agent SIFR v0.1 handshake and execution sequence.}
\label{fig:handshake}
\end{figure}

\begin{figure}[t]
\centering
\includegraphics[width=\linewidth]{figures/audit_dag.png}
\caption{Audit DAG for the implemented vertical slice.}
\label{fig:dag}
\end{figure}

\section{Prototype Implementation}
The artifact is organized as a Python package with examples, tests, benchmarks, documentation, paper source, and review materials. Table~\ref{tab:implementation} summarizes the implementation status. The complete demo creates two agents: \texttt{did:sifr:planner} and \texttt{did:sifr:executor}. The planner sends a signed Hello. The executor sends a CapabilityOffer and a signed CapabilityGrant allowing \texttt{tool.calculator.add}. The planner sends a signed Action requesting addition of 2 and 3. The executor verifies the planner's signature, verifies the grant, enforces capability checks, records ToolUse, executes the calculator stub, returns a signed Observation, and verifies the resulting audit DAG.

\begin{table}[t]
\caption{Implementation Status}
\label{tab:implementation}
\centering
\begin{tabular}{llll}
\toprule
Component & v0.1 & v0.2 & Evidence\\
\midrule
Typed envelope & Impl. & Impl. & \texttt{sifr/messages.py}\\
Ed25519 signing & Impl. & Impl. & \texttt{sifr/crypto.py}\\
Capability grants & Impl. & Impl.+R/R & \texttt{sifr/capabilities.py}\\
Audit DAG & Impl. & Impl. & \texttt{sifr/audit\_dag.py}\\
Local transport & Impl. & Impl. & \texttt{sifr/transport/local.py}\\
TensorFrame encoding & Demo & Demo & \texttt{sifr/tensor.py}\\
Calculator tool & Python stub & WASM (wasmtime) & \texttt{sifr/wasm\_runner.py}\\
QUIC transport & Future & aioquic & \texttt{sifr/transport/quic.py}\\
WASM isolation & Future & Sandboxed & \texttt{sifr/wasm\_runner.py}\\
Key management & In-mem & Argon2id+AES-GCM & \texttt{sifr/key\_management.py}\\
DID resolution & Strings only & did:web, did:sifr & \texttt{sifr/did/}\\
Cap. credentials & N/A & VC-inspired & \texttt{sifr/credentials.py}\\
Revocation & Future & Signed registry & \texttt{sifr/revocation.py}\\
Replay protection & Future & Sliding window & \texttt{sifr/replay.py}\\
Adversary eval & 5 inline & 11-attack suite & \texttt{tests/test\_network\_adversary.py}\\
Formal model & None & TLA+, 7 invariants & \texttt{formal/sifr\_capability.tla}\\
\bottomrule
\end{tabular}
\par\medskip
\footnotesize{Impl.+R/R = original implementation extended with revocation and replay checks.}
\end{table}

\section{Security Model and Threat Analysis}
SIFR v0.1 assumes that each verifier already has the correct public key for the relevant agent. This is a significant limitation: the prototype tests frame integrity and capability enforcement, not production identity discovery. The security-oriented prototype goal is to reject tampered frames, unsigned frames, wrong-key verification, expired grants, wrong-subject grants, unauthorized actions, over-budget actions, and simple audit-DAG mutation.

Listing~\ref{lst:verify} summarizes the implemented verifier logic. The key point is that signature verification and capability authorization are separate checks: a correctly signed Action can still be rejected if it lacks the right grant.

\begin{lstlisting}[caption={Capability and DAG verification sketch for SIFR v0.1.},label={lst:verify}]
authorize(action, grant, issuer_pubkey, store):
  verify_signature(action, sender_pubkey)
  verify_signature(grant, issuer_pubkey)
  require grant.payload.issuer == grant.sender_id
  require action.capability_id == grant.capability_id
  require grant.subject == action.sender_id
  require action.payload.action in grant.actions
  require now_utc < grant.expires_at
  require size(action.payload) <= grant.max_payload_bytes
  require store.calls(grant.id) < grant.max_calls
  require not action.delegate_to or grant.allow_delegation
  store.consume(grant.id)
  return ALLOW

verify_dag(dag):
  for node in dag.nodes:
    require sha256(canonical(node.message)) == node.cid
    require node.signature_valid
    for parent in node.parents:
      require parent in dag.nodes
  return OK
\end{lstlisting}

\begin{table}[t]
\caption{Threat Handling in SIFR v0.1}
\label{tab:threats}
\centering
\begin{tabular}{llll}
\toprule
Threat & v0.1 & v0.2 mechanism & Status\\
\midrule
Payload tampering & Tested & Ed25519 verify & Tested\\
Sender tampering & Tested & Signed sender field & Tested (attack 5)\\
Wrong key (kid swap) & Tested & DID-resolved verify & Tested (attack 6)\\
Unsigned message & Tested & Signature required & Tested\\
Unauthorized action & Tested & Capability action check & Tested (attack 7)\\
Wrong subject & Tested & Capability subject check & Tested\\
Expired grant & Tested & UTC expiration check & Tested (attack 3)\\
Over budget & Tested & Call counter + payload size & Tested (attack 10)\\
DAG mutation & Tested & CID recomputation & Tested\\
Missing parent & Tested & Parent lookup & Tested (attack 9)\\
Replay & Future & (sender,session,msgid) cache & \textbf{Tested} (attack 2)\\
Revocation & Future & Signed registry, on-load verify & \textbf{Tested} (attack 4)\\
Sandbox escape & Stub & wasmtime, no WASI, fuel & \textbf{Tested} (attack 11)\\
Malformed frame & Reject & Strict envelope validation & \textbf{Tested} (attack 8)\\
Prompt injection & Out of scope & Out of scope & N/A\\
\bottomrule
\end{tabular}
\par\medskip
\footnotesize{The attack number references Table~\ref{tab:adversary} in Section~\ref{sec:hardening}.}
\end{table}

The prototype deliberately does not claim formal security. Signatures establish message integrity under the assumed public key, but do not establish trust by themselves. Capability grants authorize specific actions, but v0.1 lacks revocation, distributed state, concurrent budget reservations, and replay protection. The calculator tool is bounded because it performs only a structured addition operation; it is not evidence that arbitrary generated code can be safely executed.

\section{Evaluation}
\subsection{Experimental Setup}
All measurements were generated by the included scripts under \texttt{benchmarks/}. The environment metadata is stored in \texttt{benchmarks/results/environment.json}. The current raw results were generated on Windows with Python 3.14.2. The benchmarks are local microbenchmarks and should not be interpreted as distributed network performance.

\subsection{Payload Size}
Table~\ref{tab:payload} and Figure~\ref{fig:payload} compare a plain JSON action, unsigned SIFR action, signed SIFR action, TensorFrame JSON-list encoding, and TensorFrame base64 binary encoding. The signed SIFR frame is larger than a plain JSON action because it carries versioning, identifiers, parents, sender/receiver identifiers, timestamp, capability id, and signature. For the tested 384-dimensional float32 vector, base64 binary encoding is substantially smaller than a JSON float list.

\begin{table}[t]
\caption{Payload Size Benchmark}
\label{tab:payload}
\centering
\begin{tabular}{lrr}
\toprule
Case & Bytes & Ratio vs. plain\\
\midrule
Plain JSON action & 53 & 1.00\\
SIFR unsigned action & 257 & 4.85\\
SIFR signed action & 411 & 7.75\\
Tensor JSON list & 7642 & 144.19\\
Tensor base64 binary & 2214 & 41.77\\
\bottomrule
\end{tabular}
\end{table}

\begin{figure}[t]
\centering
\includegraphics[width=\linewidth]{figures/benchmark_payload.png}
\caption{Payload-size benchmark generated from raw CSV results.}
\label{fig:payload}
\end{figure}

\subsection{Signature Overhead}
The Ed25519 microbenchmark creates, signs, and verifies 100, 1000, and 10000 messages. At 10000 messages, average signing time was 0.0314 ms per message and average verification time was 0.0615 ms per message. Figure~\ref{fig:signature} plots average cost across message counts. These measurements support H4 only for local CPU overhead, not for networked deployment.

\begin{figure}[t]
\centering
\includegraphics[width=\linewidth]{figures/benchmark_signature_overhead.png}
\caption{Ed25519 signing and verification overhead.}
\label{fig:signature}
\end{figure}

\subsection{Latency}
The latency benchmark compares plain local dict passing, SIFR message creation, SIFR signing plus verification, SIFR signing plus verification plus DAG append, and JSON serialization/deserialization as a simulated HTTP JSON baseline. Table~\ref{tab:latency} and Figure~\ref{fig:latency} report local latency only.

\begin{table}[t]
\caption{Local Latency Microbenchmark}
\label{tab:latency}
\centering
\begin{tabular}{lrr}
\toprule
Case & Mean ms & p95 ms\\
\midrule
Plain local dict & 0.00017 & 0.00040\\
SIFR create only & 0.00779 & 0.01810\\
SIFR sign+verify & 0.09833 & 0.12360\\
SIFR sign+verify+DAG & 0.11040 & 0.15260\\
HTTP JSON serialization & 0.00758 & 0.01360\\
\bottomrule
\end{tabular}
\end{table}

\begin{figure}[t]
\centering
\includegraphics[width=\linewidth]{figures/benchmark_latency.png}
\caption{Local latency microbenchmark.}
\label{fig:latency}
\end{figure}

\subsection{Capability Enforcement}
The capability benchmark runs six cases: authorized action, unauthorized action, expired capability, tampered capability, over-budget capability, and wrong subject. The authorized action succeeds. The remaining five cases are rejected with the expected error category. This supports H2 for the implemented two-agent calculator workflow.

\begin{figure}[t]
\centering
\includegraphics[width=\linewidth]{figures/benchmark_capability.png}
\caption{Capability-enforcement benchmark. The authorized case succeeds; negative cases are expected rejections.}
\label{fig:capability}
\end{figure}

\section{v0.2 Security Hardening}
\label{sec:hardening}
Section~\ref{sec:hardening} describes the v0.2 layers added on top of the v0.1 prototype. Each subsection cites the implementation file, the test file with positive and negative cases, and the trap-acceptance assertion that prevents an in-name-only implementation.

\subsection{Encrypted Key Management}
v0.2 stores private keys at rest in a passphrase-encrypted JSON file (\texttt{sifr/key\_management.py}). The file format binds an Argon2id-derived~\cite{biryukov2016argon2} AES-256-GCM key per keystore. The kid is bound as additional authenticated data on each entry, so swapping ciphertexts between two entries fails on decryption. Tests in \texttt{tests/test\_key\_management.py} include the trap-acceptance assertions \emph{file-does-not-contain-raw-private-key} and \emph{kid-AAD-binding-prevents-swap}. We do not claim HSM-grade isolation or enterprise PKI; the threat model is documented in \texttt{docs/key\_management.md}.

\subsection{DID Resolution}
v0.2 implements two DID methods behind a common \texttt{DidResolver} ABC (\texttt{sifr/did/}). \texttt{did:web}~\cite{didweb2024spec} fetches DID documents over HTTPS per the W3C method specification, with percent-encoded port handling. \texttt{did:sifr} reads documents from a local directory and is intended for tests and air-gapped scenarios. Both are exercised by an in-process loopback HTTP fixture (\texttt{tests/fixtures/did\_web\_server.py}) that proves did:web tests use real HTTP traffic. Trap-acceptance tests check kid-not-in-document, controller-mismatch, path-traversal, and id-mismatch reject paths. We do not claim W3C DID ecosystem interoperability beyond the supported \texttt{Ed25519VerificationKey2020} subset.

\subsection{VC-Inspired Capability Credentials and Revocation}
\texttt{sifr/credentials.py} implements signed credentials structured like W3C Verifiable Credentials (\texttt{@context}, \texttt{type}, \texttt{issuer}, \texttt{issuanceDate}, \texttt{expirationDate}, \texttt{credentialSubject}, \texttt{proof}). The proof is an \texttt{Ed25519Signature2020} over canonical JSON without the proof field. Trap-acceptance tests demonstrate that mutating \texttt{credentialSubject.id}, \texttt{expirationDate}, the embedded \texttt{capability.actions}, or \texttt{proof.proofValue} after signing all fail verification, and that the verification-method DID must match the issuer DID. We do NOT claim W3C VC compliance: there is no JSON-LD context processing and no URDNA2015 RDF normalization. The label is "VC-inspired signed credential."

\texttt{sifr/revocation.py} implements a registry of signed \texttt{CapabilityRevocation} SIFR messages with optional JSONL persistence. Entries are re-verified on load, so tampering with a stored record fails reload. \texttt{sifr/capabilities.py:authorize\_action} consults the registry before authorization; a revoked grant raises \texttt{UnauthorizedAction("REVOKED\_CAPABILITY")} (attack 4 in Table~\ref{tab:adversary}).

\subsection{Replay Protection}
\texttt{sifr/replay.py} maintains a per-process cache keyed on \texttt{(sender\_id, session\_id, message\_id)} with a sliding timestamp window (default 5 minutes). A trap-acceptance test demonstrates that re-signing the same message with a different signature value still fails replay because the cache key does not include the signature. Optional SQLite persistence retains entries across restarts. Window-edge cases (stale, future, exact boundary) are tested.

\subsection{WASM Tool Isolation}
\texttt{sifr/wasm\_runner.py} replaces the v0.1 Python calculator stub with \texttt{wasmtime}~\cite{wasmtime}-based execution of \texttt{wasm/calculator/calculator.wat}. The runner does NOT link any WASI imports; modules importing \texttt{wasi\_snapshot\_preview1} fail to instantiate. Each call uses a fresh \texttt{Store} with a fixed fuel budget (default 1{,}000{,}000), bounding compute and trapping infinite loops. The committed module is WAT (text), not opaque \texttt{.wasm}, so reviewers can read the 13-line source.

Trap-acceptance assertions in \texttt{tests/test\_wasm\_runner.py}: (i) a parity test of 8 input pairs proves Python and WASM produce bit-identical results; (ii) every successful call advances \texttt{last\_invocation\_evidence["fuel\_consumed"]} (a Python fall-through would never advance it); (iii) the \texttt{fs\_attempt.wat} fixture, which tries to import \texttt{path\_open}, fails to instantiate; (iv) the \texttt{looping.wat} fixture traps on fuel.

\subsection{QUIC Transport}
\texttt{sifr/transport/quic.py} provides a single-stream length-prefixed canonical-JSON transport over \texttt{aioquic}~\cite{aioquic} 1.3 with ALPN \texttt{sifr/0.2}. Self-signed cert generation for tests and demos is implemented in \texttt{sifr/transport/\_certs.py}; private keys are not committed. The trap-acceptance test asserts \texttt{isinstance(transport.quic\_connection, aioquic.quic.connection.QuicConnection)} AND \texttt{negotiated\_alpn == "sifr/0.2"} AND that a real \texttt{original\_destination\_connection\_id} was negotiated --- ruling out a TCP look-alike.

Loopback RTT for sign+verify+DAG over LocalTransport is 0.20 ms; over QUIC the same workload takes 0.94 ms (\texttt{benchmarks/results/quic\_latency.csv}); the difference reflects QUIC handshake amortization, framing, and asyncio scheduling overhead.

\subsection{Network Adversary Evaluation}
\texttt{tests/test\_network\_adversary.py} encodes 11 controlled attacks. Each test asserts (a) the exact rejection error class and (b) that \texttt{WasmToolRunner.last\_invocation\_evidence} is unchanged after the rejection, proving the attack never reached the tool. Reject latencies (200 trials each, mean) are in Table~\ref{tab:adversary}. The replay attack is slow because it includes one legitimate authorize+WASM call before the replay attempt.

\begin{table}[t]
\caption{Network Adversary Rejection Latencies}
\label{tab:adversary}
\centering
\begin{tabular}{rlr}
\toprule
\# & Attack and expected error & Mean reject ($\mu$s)\\
\midrule
1 & Tampered body $\to$ SignatureError & 202\\
2 & Replay $\to$ ReplayError & 2397\\
3 & Expired grant $\to$ UnauthorizedAction(EXPIRED) & 793\\
4 & Revoked grant $\to$ UnauthorizedAction(REVOKED) & 527\\
5 & Swap sender\_id $\to$ SignatureError & 203\\
6 & Swap kid $\to$ SignatureError & 209\\
7 & Unauthorized action $\to$ UnauthorizedAction(UNAUTH.) & 494\\
8 & Malformed frame $\to$ MessageValidationError & 11\\
9 & Drop parent DAG node $\to$ AuditDAGError & 227\\
10 & Oversized payload $\to$ UnauthorizedAction(BUDGET) & 546\\
11 & WASM execution without grant $\to$ CapabilityError & 2\\
\bottomrule
\end{tabular}
\par\medskip
\footnotesize{Source: \texttt{benchmarks/results/adversary\_rejection.json}.}
\end{table}

This is a controlled adversary evaluation under the v0.2 trust assumptions. It is not a fuzz test, a penetration test, or a survey of all possible attacks.

\subsection{TLA+ Formal Model}
\texttt{formal/sifr\_capability.tla} models the capability authorization state machine in TLA+~\cite{lamport2002tla}, checked with TLC~\cite{tlatools}: states \texttt{unissued} $\to$ \texttt{active} $\to$ \{\texttt{expired}, \texttt{revoked}\}; actions \texttt{Issue}, \texttt{Expire}, \texttt{Revoke}, \texttt{Consume}; replay set \texttt{(subject, message\_id)}; bounded budget. Seven invariants (Table~\ref{tab:invariants}) jointly express safety: no over-budget, wrong-subject, unauthorized-action, replayed, post-revocation, or post-expiration consume.

\begin{table}[t]
\caption{Formal Invariants and Their Implementation Witnesses}
\label{tab:invariants}
\centering
\small
\begin{tabular}{p{0.40\linewidth}p{0.50\linewidth}}
\toprule
TLA+ Invariant & Code witness (raises on violation)\\
\midrule
\texttt{NoOverBudgetConsume} & \texttt{authorize\_action} $\to$ \texttt{CALL\_BUDGET\_EXCEEDED}\\
\texttt{NoWrongSubjectConsume} & \texttt{authorize\_action} $\to$ \texttt{WRONG\_SUBJECT}\\
\texttt{NoUnauthorizedActionConsume} & \texttt{authorize\_action} $\to$ \texttt{UNAUTHORIZED\_ACTION}\\
\texttt{NoReplayedConsume} & \texttt{ReplayCache.check\_and\_record} $\to$ \texttt{ReplayError}\\
\texttt{NoConsumeAfterRevoke} & \texttt{authorize\_action} $\to$ \texttt{REVOKED\_CAPABILITY}\\
\texttt{NoConsumeAfterExpire} & \texttt{authorize\_action} $\to$ \texttt{EXPIRED\_CAPABILITY}\\
\texttt{TypeInvariant} & Static guarantee of the data model\\
\bottomrule
\end{tabular}
\end{table}

TLC v2026.05.04 with 16 workers explored \textbf{276{,}205 distinct states} at depth 9 in 7 seconds against the bounded constants in \texttt{formal/MC.cfg} (\texttt{Caps=\{c1,c2\}}, \texttt{Subs=\{alice,bob\}}, \texttt{Acts=\{add,multiply\}}, \texttt{Msgs=\{m1,m2\}}, \texttt{MaxCalls=2}). TLC reports \emph{Model checking completed. No error has been found.} Output is committed at \texttt{formal/output/tlc\_output.txt}; \texttt{tests/test\_formal\_artifacts.py} parses it and skips with explicit instructions when no Java/TLC is available locally.

We do NOT claim cryptographic proof, liveness verification, or implementation/specification equivalence. The model captures the spec of authorization; the trap-acceptance tests in \texttt{tests/test\_network\_adversary.py} are the bridge between spec and implementation.

\subsection{End-to-End Vertical Slice}
\texttt{examples/demo\_secure\_quic\_wasm\_did\_flow.py} runs the full v0.2 flow over real QUIC: two agents resolve each other's DID documents, establish a TLS-secured QUIC connection, exchange signed Hellos, the issuer (Bob) issues a VC-inspired credential, the subject (Alice) verifies it, Alice signs an Action, Bob runs replay+revocation+authorization checks, Bob runs the WASM calculator, Bob signs the Observation, both build an audit DAG, and the DAG verifies. The demo exits 0 with the spec's expected output:

\begin{lstlisting}[basicstyle=\ttfamily\scriptsize]
=== SIFR v0.2 Secure Flow Demo ===
DID resolution: OK
QUIC session: OK
Hello signature: OK
Capability credential: OK
Replay check: OK
Revocation check: OK
Action authorized: OK
WASM calculator executed: OK
Observation verified: OK
Audit DAG integrity: OK
Formal model artifacts: PRESENT
Result: 5
Demo completed successfully.
\end{lstlisting}

The companion test \texttt{tests/test\_secure\_flow.py} subprocess-launches the demo and asserts each required \texttt{OK} line is present and the exit code is 0.

\section{Discussion}
The prototype demonstrates that a compact vertical slice can bind communication semantics, signatures, capabilities, tool execution, and audit lineage. This is the main value of SIFR v0.1: not that the protocol is complete, but that the implementation makes a research claim testable. A reviewer can run the demo, tamper with frames, expire grants, exceed budgets, remove DAG parents, regenerate figures, and inspect the resulting behavior.

The v0.2 hardening converts each ``Future'' entry from the v0.1 limitations table into ``Tested'' with cited code, tests, benchmarks, and (in the case of authorization invariants) a model-checked TLA+ artifact. Concrete numbers: the v0.2 demo exchanges seven SIFR messages over a real QUIC stream with sub-millisecond per-message overhead at the application layer; the WASM calculator runs in $\sim$113 $\mu$s warm and produces an audit fuel-consumption counter that distinguishes WASM execution from a Python fall-through; revocation lookups are $\sim$0.3 $\mu$s regardless of registry size; and the 11-attack adversary suite shows reject latencies bounded by 2.4 ms even for the slowest path (replay, which subsumes a legitimate authorize+execute round).

The results also show costs and trade-offs. Signed frames are larger than plain JSON actions. Ed25519 verification is slower than signing in the measured environment. DAG append adds additional local overhead. These costs may be acceptable for integrity-sensitive agent workflows, but the current evaluation is too small to justify broad performance claims. The TensorFrame experiment demonstrates encoding-size differences only; it does not demonstrate privacy, semantic quality, compression efficiency, or safe KV-cache sharing.

\section{Limitations}
v0.2 closes the v0.1 ``future work'' list at the artifact level but introduces its own scope boundary. We do not claim: W3C VC compliance (no JSON-LD context processing, no URDNA2015 RDF normalization), enterprise PKI or HSM-grade key isolation, distributed revocation propagation (the registry is per-process; gossip and consensus are out of scope), arbitrary-untrusted-code WASM safety (the sandbox is verified for the calculator and two adversarial fixtures only), wall-clock timeouts on WASM (fuel bounds compute, not seconds), liveness or cryptographic proof from the TLA+ model (only safety, only against a bounded model), production deployment readiness, fuzz coverage, side-channel resistance (timing/cache/Spectre-class), or interoperability with MCP, A2A, ACP, or ANP ecosystems beyond protocol-shape similarity. The replay cache uses a per-process \texttt{dict}; its garbage collection is $O(n)$ per check and characterized as a known cost in \texttt{benchmarks/results/replay\_overhead.csv}.

\section{Future Work}
With QUIC, DID, VC, revocation, replay, WASM, formal modeling, and adversary evaluation now in the artifact, future work shifts up the stack. Concrete next steps include: distributed revocation gossip; multi-process concurrency-safe budget reservation; HSM/keyring-backed key storage as a default; richer formal modeling (cryptographic protocols via ProVerif/Tamarin; liveness via TLC fairness annotations); a wall-clock interrupt for WASM via wasmtime epoch interruption; multi-tenant sandbox isolation; fuzz testing on the message validation and authorization paths; protocol adapters bridging SIFR frames to MCP, A2A, ACP, and ANP carriers; privacy-preserving TensorFrame designs informed by embedding inversion and KV-cache leakage research; and CI matrix expansion to cover macOS in addition to the current Ubuntu and Windows runners.

\section{Conclusion}
SIFR v0.1 proposes and implements a narrow but complete vertical slice for structured, signed, capability-checked AI-agent communication. SIFR v0.2 promotes the prototype to a verified research artifact: encrypted-at-rest key management; DID resolution; VC-inspired credentials with mutation defenses; signed revocation; replay protection; sandboxed WASM execution; real QUIC transport; an 11-attack adversary suite; and a TLA+ model checked against 276{,}205 states under 7 invariants. The end-to-end demo exits 0 with the spec's expected output. Across both iterations the work remains deliberately scoped and not production-ready, but it provides a concrete, testable baseline for agent-native protocol research with explicit non-claims at each layer.

\section*{Reproducibility Statement}
The artifact includes source code, tests, benchmark scripts, raw benchmark files (CSV and JSON), generated plots, paper source, formal model files, and review notes. From the repository root, run:
\begin{verbatim}
pip install -e ".[dev]"
pytest
python examples/demo_secure_quic_wasm_did_flow.py
python benchmarks/bench_did_resolution.py
python benchmarks/bench_quic_latency.py
python benchmarks/bench_adversary_rejection.py
python benchmarks/bench_wasm_overhead.py
python benchmarks/bench_credential_verification.py
python benchmarks/bench_replay_overhead.py
python benchmarks/bench_revocation_overhead.py
\end{verbatim}
The TLA+ model can be checked by any reviewer with a Java JRE 11+ and \texttt{tla2tools.jar}:
\begin{verbatim}
pwsh scripts/install_tla.ps1   # downloads tla2tools.jar
pwsh formal/run_tlc.ps1        # writes formal/output/tlc_output.txt
\end{verbatim}
The bash equivalents are \texttt{bash formal/run\_tlc.sh}. The Overleaf-ready paper is in \texttt{paper/}; v0.1 benchmark results are preserved under \texttt{benchmarks/results/v0.1/}; v0.2 results are at the top level of \texttt{benchmarks/results/}. CI configuration in \texttt{.github/workflows/test.yml} runs the test matrix on Ubuntu and Windows for Python 3.11 and 3.12.

\bibliographystyle{IEEEtran}
\bibliography{references}

\end{document}
